import 'package:get/get.dart';

/// Auth Feature Dependencies Binding
class AuthBinding extends Bindings {
  @override
  void dependencies() {
    // Auth API（无状态，无需注册）
    // User API（无状态，无需注册）
  }
}
